syntax = "proto3";
package klyraprotocol.yields;

option go_package = "github.com/StreamFinance-Protocol/stream-chain/protocol/x/yield/types";

// GenesisState defines the yield module's genesis state.
message GenesisState {}
